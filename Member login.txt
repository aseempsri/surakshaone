<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Log in — SurakshaOne</title>
<meta name="description" content="Log in to your SurakshaOne member account.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#111826; --ink-soft:#666c78; --mute:#8b919c;
    --brand-blue:#2c5fdd; --brand-blue-deep:#3738ac; --brand-cyan:#00b0d6;
    --paper:#ffffff; --paper-2:#f2f5fb;
    --line:#e5e8ed; --line-2:rgba(17,24,38,.08);
    --sh:0 8px 28px rgba(44,95,221,.10);
    --sh-lg:0 24px 60px rgba(44,95,221,.14);
    --maxw:1180px;
  }
  *{box-sizing:border-box}
  body{margin:0;font-family:'Inter',system-ui,sans-serif;color:var(--ink);background:var(--paper);line-height:1.55;-webkit-font-smoothing:antialiased}
  h1{font-family:'Inter',system-ui,sans-serif;margin:0;line-height:1.12;letter-spacing:-.02em;font-weight:700}
  a{color:inherit;text-decoration:none}
  img{max-width:100%;display:block}
  button{font-family:inherit;cursor:pointer}
  header{position:sticky;top:0;z-index:50;background:rgba(255,255,255,.86);backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);border-bottom:1px solid var(--line)}
  .navbar{max-width:var(--maxw);margin:0 auto;padding:13px 28px;display:flex;align-items:center;justify-content:space-between}
  .brand{display:flex;align-items:center;gap:10px}
  .brand img{height:34px;width:auto}
  .brand-name{font-weight:700;font-size:19px}
  .brand-name b{color:var(--brand-blue)}
  .nav-right a{font-size:14.5px;font-weight:600;color:var(--ink-soft)}
  .nav-right a:hover{color:var(--ink)}

  .login-shell{
    min-height:calc(100vh - 62px);
    display:grid;place-items:center;
    padding:48px 20px 64px;
    background:
      radial-gradient(ellipse 70% 55% at 15% 10%, rgba(44,95,221,.14), transparent 55%),
      radial-gradient(ellipse 55% 45% at 95% 90%, rgba(0,176,214,.12), transparent 50%),
      linear-gradient(180deg, #f7f9ff 0%, #ffffff 48%, #f2fbfd 100%);
  }
  .login-frame{
    width:100%;max-width:920px;
    display:grid;grid-template-columns:1.05fr .95fr;
    border-radius:28px;overflow:hidden;
    border:1px solid rgba(44,95,221,.12);
    box-shadow:var(--sh-lg);
    background:#fff;
  }
  .login-story{
    padding:44px 40px 40px;
    background:
      linear-gradient(160deg, rgba(55,56,172,.96), rgba(44,95,221,.92) 48%, rgba(0,176,214,.88));
    color:#fff;
    display:flex;flex-direction:column;justify-content:space-between;gap:36px;
    position:relative;overflow:hidden;
  }
  .login-story::after{
    content:"";position:absolute;inset:auto -20% -30% 20%;height:70%;
    background:radial-gradient(circle, rgba(255,255,255,.18), transparent 60%);
    pointer-events:none;
  }
  .story-top{position:relative;z-index:1}
  .story-kicker{
    display:inline-flex;align-items:center;gap:8px;
    font-size:12px;font-weight:600;letter-spacing:.08em;text-transform:uppercase;
    opacity:.9;margin-bottom:18px;
  }
  .story-kicker::before{content:"";width:7px;height:7px;border-radius:50%;background:#fff}
  .login-story h2{
    margin:0 0 12px;font-size:clamp(28px,3.4vw,36px);font-weight:600;
    letter-spacing:-.03em;line-height:1.15;max-width:12ch;
  }
  .login-story p{margin:0;font-size:15.5px;line-height:1.55;opacity:.92;max-width:30ch}
  .story-points{list-style:none;margin:0;padding:0;display:grid;gap:12px;position:relative;z-index:1}
  .story-points li{
    display:flex;gap:12px;align-items:flex-start;
    font-size:13.5px;line-height:1.4;opacity:.95;
  }
  .story-points .dot{
    width:8px;height:8px;border-radius:50%;background:#fff;flex-shrink:0;margin-top:6px;opacity:.85;
  }

  .login-panel{padding:44px 40px 40px;display:flex;flex-direction:column;justify-content:center}
  .login-panel .eyebrow{
    display:inline-flex;align-items:center;gap:8px;
    font-size:12px;letter-spacing:.08em;text-transform:uppercase;
    color:var(--brand-blue);font-weight:600;margin-bottom:12px;
  }
  .login-panel .eyebrow::before{
    content:"";width:7px;height:7px;border-radius:50%;
    background:linear-gradient(135deg,var(--brand-blue),var(--brand-cyan));
  }
  .login-panel h1{font-size:clamp(26px,3vw,32px);font-weight:600;margin-bottom:8px}
  .login-panel .sub{margin:0 0 28px;font-size:15px;color:var(--ink-soft)}

  .field{margin-bottom:16px}
  .field label{display:block;font-size:13px;font-weight:600;margin-bottom:8px;color:var(--ink)}
  .field-shell{
    display:flex;align-items:center;gap:10px;
    padding:4px 4px 4px 14px;
    border-radius:14px;border:1.5px solid var(--line);
    background:var(--paper-2);
    transition:border-color .15s, background .15s, box-shadow .15s;
  }
  .field-shell:focus-within{
    background:#fff;border-color:var(--brand-blue);
    box-shadow:0 0 0 4px rgba(44,95,221,.12);
  }
  .field-shell .ico{
    width:18px;height:18px;flex:0 0 18px;color:var(--mute);display:grid;place-items:center;
  }
  .field-shell .ico svg{width:18px;height:18px;stroke:currentColor;fill:none;stroke-width:1.8}
  .field-shell input{
    flex:1;min-width:0;width:100%;padding:12px 12px 12px 0;
    border:none;background:transparent;font:inherit;font-size:15px;font-weight:500;color:var(--ink);
  }
  .field-shell input:focus{outline:none}
  .field-shell input::placeholder{color:#a0a6b2;font-weight:400}

  .err{
    display:none;margin:0 0 16px;padding:12px 14px;border-radius:12px;
    background:#fff1f1;border:1px solid #ffd0d0;color:#b42318;font-size:13.5px;line-height:1.4;
  }
  .err.on{display:block}

  .btn{
    display:block;width:100%;margin-top:8px;padding:15px 20px;border-radius:100px;border:none;
    background:linear-gradient(135deg,var(--brand-blue-deep),var(--brand-blue) 55%,var(--brand-cyan));
    color:#fff;font-weight:600;font-size:15px;
    box-shadow:0 12px 28px rgba(44,95,221,.28);
    transition:transform .15s, box-shadow .15s;
  }
  .btn:hover{box-shadow:0 16px 34px rgba(44,95,221,.34);transform:translateY(-1px)}
  .btn:active{transform:none}

  .panel-foot{
    margin-top:22px;padding-top:18px;border-top:1px solid var(--line);
    display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;
    font-size:13.5px;color:var(--ink-soft);
  }
  .panel-foot a{font-weight:600;color:var(--brand-blue)}
  .panel-foot a:hover{color:var(--brand-blue-deep)}

  @media (max-width:820px){
    .login-frame{grid-template-columns:1fr;max-width:440px}
    .login-story{padding:28px 24px 24px;gap:22px}
    .login-story h2{max-width:none;font-size:26px}
    .story-points{display:none}
    .login-panel{padding:28px 24px 26px}
  }
</style>
</head>
<body>
<header>
  <div class="navbar">
    <a href="index.html" class="brand"><img src="/logo.png" alt="SurakshaOne"><span class="brand-name">Suraksha<b>One</b></span></a>
    <div class="nav-right"><a href="index.html">Back to site</a></div>
  </div>
</header>
<main class="login-shell">
  <div class="login-frame">
    <aside class="login-story" aria-hidden="true">
      <div class="story-top">
        <div class="story-kicker">SurakshaOne</div>
        <h2>Your five protections. One login.</h2>
        <p>Open LifeSecure and the rest of your membership from the web.</p>
      </div>
      <ul class="story-points">
        <li><span class="dot"></span><span>LifeSecure Family Protection Report</span></li>
        <li><span class="dot"></span><span>One household dashboard</span></li>
        <li><span class="dot"></span><span>Works entirely in the browser</span></li>
      </ul>
    </aside>
    <div class="login-panel">
      <div class="eyebrow">Members</div>
      <h1>Welcome back</h1>
      <p class="sub">Sign in to continue to your member area.</p>
      <p class="err" id="loginError" role="alert">That ID or password is wrong. Please try again.</p>
      <form id="memberLogin" autocomplete="on">
        <div class="field">
          <label for="loginId">Member ID</label>
          <div class="field-shell">
            <span class="ico" aria-hidden="true"><svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="3.2"/><path d="M5 19c0-3.2 3-5.5 7-5.5s7 2.3 7 5.5" stroke-linecap="round"/></svg></span>
            <input id="loginId" name="username" type="text" required autocomplete="username" placeholder="Your member ID">
          </div>
        </div>
        <div class="field">
          <label for="loginPassword">Password</label>
          <div class="field-shell">
            <span class="ico" aria-hidden="true"><svg viewBox="0 0 24 24"><rect x="5" y="11" width="14" height="9" rx="2"/><path d="M8 11V8a4 4 0 0 1 8 0v3" stroke-linecap="round"/></svg></span>
            <input id="loginPassword" name="password" type="password" required autocomplete="current-password" placeholder="Your password">
          </div>
        </div>
        <button type="submit" class="btn">Log in</button>
      </form>
      <div class="panel-foot">
        <span>New here?</span>
        <a href="signup.html">Sign up — coming soon</a>
      </div>
    </div>
  </div>
</main>
</body>
</html>
